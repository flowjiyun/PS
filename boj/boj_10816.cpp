#include <iostream>
#include <cmath>

using namespace std;

int main(void)
{
    ios:: sync_with_stdio(0);
    cin.tie(0);
    int n = 1024;
    double result = log2(n);
    cout << result;
    return (0);

}